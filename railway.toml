[build]
builder = "nixpacks"
buildCommand = "pip install --no-cache-dir -r requirements.txt"

[build.nixpacks]
providers = ["python", "pip"]

[deploy]
startCommand = "uvicorn api:app --host 0.0.0.0 --port $PORT"
